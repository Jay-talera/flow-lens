server:
  port: ${SERVER_PORT:10000}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-test-query: "SELECT 1"
      initialization-fail-timeout: 0

  flyway:
    enabled: true
    baseline-on-migrate: true
    url: ${DB_URL}                    # ← explicitly set flyway url
    user: ${DB_USER:postgres}         # ← explicitly set flyway user
    password: ${DB_PASSWORD:postgres} # ← explicitly set flyway password

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate.jdbc.time_zone: UTC
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

github:
  token: ${GITHUB_TOKEN}

frontend:
  url: ${FRONTEND_URL:http://localhost:5173}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never